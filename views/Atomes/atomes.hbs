<div class="center">
    <h1> Express App : {{title}}</h1>
    <br>
    <div class="inputbox">
        <select name="atome" id="atome">
            {{#compare data null operator="=="}}
                <option value="rien">Il n'y a pas de données dans la base atome</option>
            {{else}}
                <option value="0">Sélectionnez un atome</option>
                {{#each data}}
                    <option value="{{this.id_users}}">{{this.name}} {{this.firstName}}-{{this.function}}</option>
                {{/each}}
            {{/compare}}
        </select>
    </div>
    <h2>or</h2><br />
    <div class="inputbox">
        <button id="newUser">Creéer un atome</button>
    </div>
</div>
<script>
    var usr = document.getElementById('user');
    usr.addEventListener('change', function (evt) {
        window.location = "/formUser/" + usr.value;
    });
    var btn_new = document.getElementById('newUser');
    btn_new.addEventListener('click', function (evt) {
        window.location = "/newUser";
    });
</script>